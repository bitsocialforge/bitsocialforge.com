interface:
  display_name: "Review and Merge PR"
  short_description: "Review PR feedback and readiness within the requested scope"
  default_prompt: "Review the current PR and report its readiness; apply fixes or merge only when requested."
