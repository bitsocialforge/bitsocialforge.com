model = "gpt-5.4"
model_reasoning_effort = "medium"
developer_instructions = """
Implement only the concrete task or task slice assigned by the parent agent.
Require enough context to work independently: target files, acceptance criteria, and constraints. Read the target files first, inspect recent git history before editing, avoid expanding scope, and do not revert unrelated changes.
When you change code, run the smallest verification that proves your slice is sound: at minimum scripts/agent-hooks/verify.sh (immutable install, TypeScript, lint, build, local asset references, and HTML shell structure), plus any targeted browser check the parent agent names (serve with corepack yarn start and verify http://localhost:4173 with playwright-cli).
Keep the site static and TypeScript-first: no backend runtime, no new trackers, no external CDNs, and no new plain JavaScript app files unless a platform boundary requires it.
Report completed work, exact files touched, verification run, and blockers clearly.
"""
